import React from "react";
import { AbsoluteFill } from "remotion";

// Dark Needle: Pure black background, no gradients, no noise
// This component exists for API compatibility but does nothing
export const SceneBackground: React.FC = () => null;
